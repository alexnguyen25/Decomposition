demucs==4.0.1
librosa>=0.10.0
certifi>=2024.0.0
# Torch 2.11+ torchaudio requires torchcodec + FFmpeg dylibs; 2.2.x uses classic loaders.
torch==2.2.2
torchaudio==2.2.2
numpy>=1.26,<2
soundfile>=0.12.1  # torchaudio.save WAV needs a backend (ffmpeg/sox ext or soundfile)
diffq>=0.2.1  # required for quantized models (mdx_q, mdx_extra_q, …)
matplotlib>=3.8.0
